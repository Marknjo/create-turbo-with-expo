import { ResponseWrapperInterceptor } from './response-wrapper.interceptor'

describe('ResponseWrapperInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseWrapperInterceptor()).toBeDefined()
  })
})
